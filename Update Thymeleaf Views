<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Student List</title>
</head>
<body>
<h2>Students</h2>

<form th:action="@{/students}" method="post">
    <label>Student ID:</label>
    <input type="text" name="studentId" required />
    <label>CGPA:</label>
    <input type="number" step="0.01" name="cgpa" required />
    <button type="submit">Add Student</button>
</form>

<br>

<table border="1">
    <thead>
    <tr>
        <th>ID</th>
        <th>Student ID</th>
        <th>CGPA</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <tr th:each="student : ${students}">
        <td th:text="${student.id}"></td>
        <td th:text="${student.studentId}"></td>
        <td th:text="${student.cgpa}"></td>
        <td>
            <a th:href="@{'/students/edit/' + ${student.id}}">Update</a>
            <form th:action="@{'/students/' + ${student.id}}" method="post" style="display:inline;">
                <button type="submit">Delete</button>
            </form>
        </td>
    </tr>
    </tbody>
</table>

<p>Date and Time: <span th:text="${#dates.format(new java.util.Date(), 'yyyy-MM-dd HH:mm:ss')}"></span></p>
</body>
</html>
